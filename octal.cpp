#include <stdio.h>

int main(void)
{
	int x = 100;
	printf("10진수로 출력 : %d\n", x);
	printf("8진수로 출력 : %o\n", x); //%o는 8진수로 표현하는 것이다. 
	printf("16진수로 출력 : %x", x); // %x는 16진수로 표현하는 것이다. 
	return 0;
 } 
